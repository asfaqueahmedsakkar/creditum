// ignore_for_file: constant_identifier_names

enum AccountType { mobile_banking, card, cash, none }

enum TransactionType { transfer, debit, credit }

enum LoanType { loan, loan_payment, lend, lend_payment_receive }
